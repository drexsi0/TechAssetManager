@{
    if (ViewData.TryGetValue("ParentLayout", out var parentLayoutObj) && parentLayoutObj is string parentLayoutStr && parentLayoutStr is not null)
    {
        Layout = parentLayoutStr;
    }
    else
    {
        Layout = "/Areas/Identity/Pages/_Layout.cshtml";
    }
}

<h1>Gerenciar sua conta</h1>

<div>
    <h2>Alterar suas configurações de conta</h2>
    <hr />
    <div class="row">
        <div class="col-md-3">
            <partial name="_ManageNav" />
        </div>
        <div class="col-md-9">
            @RenderBody()
        </div>
    </div>
</div>

@section Scripts {
    @RenderSection("Scripts", required: false)
}